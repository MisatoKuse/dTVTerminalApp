<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/channel_list_background_color"
    android:orientation="vertical">
    <ListView
        android:id="@+id/main_setting_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@color/setting_list_divider_color"
        android:dividerHeight="@dimen/main_setting_list_divider_height"
        android:background="@color/setting_list_background_color">
    </ListView>
</LinearLayout>
